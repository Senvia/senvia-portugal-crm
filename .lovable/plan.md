

## Plano: Remover texto da regra de derivação no resumo do CPE

### Alteração
- **Ficheiro:** `src/components/proposals/ProposalCpeSelector.tsx`
- Remover as linhas 395-399 que exibem o `getTierRuleLabel` abaixo da margem calculada
- Remover o import de `getTierRuleLabel` se não for usado noutro lugar do ficheiro

