# Dependency remediation, 2026-09-11

All three npm dependency trees now report zero known advisories. Receipts: `npm-audit-root-after.json`, `npm-audit-gateway-after.json`, `npm-audit-extension-after.json`. Root Bun lock was regenerated by importing the audited npm lock in an isolated temporary directory; `bun audit --json` returned `{}`.

Root: Vite 6.4.3, React Router DOM 7.18.3, DOMPurify 3.4.15, SheetJS 0.20.3. React remains 18. React Router previously enabled both v7 future flags; these obsolete props were removed from App.tsx. jsdom and PGlite are development-only regression-test dependencies. Root and extension Supabase packages require Node 22 or newer; verification runtime is Node 24.14.1.

Gateway: Fastify 5.12.3, Nodemailer 10.0.3, ImapFlow 1.7.8, Mailparser 3.9.24, ipaddr.js 2.5.0. Existing Node 20 image meets gateway dependencies. Fastify/Nodemailer major upgrades are necessary to obtain currently patched releases. Gateway behavior tests must cover their integration.

Extension: Vite 6.4.3 and patched transitive dependencies.

SheetJS uses the authoritative distribution documented at https://docs.sheetjs.com/docs/getting-started/installation/nodejs/ rather than the abandoned npm registry release. URL: https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz . Locked SHA-512 integrity: `sha512-oLDq3jw7AcLqKWH2AhCpVTZl8mf6X2YReP+Neh0SJUzV/BdZYjth94tG5toiMB1PPrYtxOCfaoUCkvtuH+3AJA==`. In-memory XLSX write/read roundtrip preserved accented text and numeric values.

No forced audit fix was used. Normal npm lifecycle behavior was enabled; build-tool packages declare install scripts. Bun lock generation disabled scripts. Audit results are time-bound advisory database checks, not proof of absence of vulnerabilities. Parent task owns integrated application builds and runtime verification.
